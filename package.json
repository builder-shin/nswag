{
  "name": "nswag-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "OpenAPI spec generation and documentation tool monorepo for Node.js",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "build:specs": "pnpm --filter @builder-shin/nswag-specs build",
    "build:api": "pnpm --filter @builder-shin/nswag-api build",
    "build:ui": "pnpm --filter @builder-shin/nswag-ui build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "turbo": "^2.3.0",
    "typescript": "^5.3.0"
  },
  "keywords": [
    "openapi",
    "swagger",
    "api-documentation",
    "testing",
    "nodejs"
  ],
  "author": "",
  "license": "MIT"
}
